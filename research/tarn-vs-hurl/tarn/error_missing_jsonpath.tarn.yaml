name: Missing JSONPath Example
env:
  base_url: "http://127.0.0.1:3001"
steps:
  - name: Missing JSONPath
    request:
      method: GET
      url: "{{ env.base_url }}/health"
    assert:
      status: 200
      body:
        "$.missing": "x"
