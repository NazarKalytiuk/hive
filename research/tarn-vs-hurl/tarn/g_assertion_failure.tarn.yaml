name: Scenario G - Assertion Failure
env:
  base_url: "http://127.0.0.1:3001"
steps:
  - name: Expect 200 from missing route
    request:
      method: GET
      url: "{{ env.base_url }}/missing"
    assert:
      status: 200
