name: Scenario A - Minimal GET
env:
  base_url: "https://httpbin.org"
steps:
  - name: GET public endpoint
    request:
      method: GET
      url: "{{ env.base_url }}/get"
    assert:
      status: 200
