name: Invalid Regex Example
env:
  base_url: "http://127.0.0.1:3001"
steps:
  - name: Invalid regex header assertion
    request:
      method: GET
      url: "{{ env.base_url }}/health"
    assert:
      status: 200
      headers:
        x-request-id: 'matches "["'
